/**
 * SpaceBackground — pure CSS implementation.
 * Stars rendered via box-shadow on pseudo-elements.
 * No JS, no runtime computation — just a div with CSS classes.
 */
export function SpaceBackground() {
  return <div className="space-bg" />;
}
